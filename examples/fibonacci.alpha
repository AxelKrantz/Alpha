fn fibonacci(n: i64) -> i64 {
    if n <= 1 {
        return n
    }
    return fibonacci(n - 1) + fibonacci(n - 2)
}

fn main() {
    let mut i: i64 = 0
    while i < 20 {
        let result = fibonacci(i)
        println(result)
        i += 1
    }
}
